.. _python-api-reference:

Python API reference
====================

Most users will find the Python interface to ``aml_storage`` to be the most
convenient to use. This interface is built on top of the C API, and can be
:ref:`installed independently <install-python-lib>`. The functions and classes
provided in ``aml_storage`` can be grouped in four main groups:

.. toctree::
    :maxdepth: 1

    tensor
    labels
    blocks
    misc
