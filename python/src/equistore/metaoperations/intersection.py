from ..labels import Labels


def intersection(a: Labels, b: Labels) -> Labels:
    """
    Compares 2 Labels objects, returns a Labels object of indices in the set
    intersection of them.
    """
    return
